import { Guides } from '@/components/Guides';
import { Resources } from '@/components/Resources';
import { HeroPattern } from '@/components/HeroPattern';

export const metadata = {
	title: 'React sprout',
	description: 'Learn how to get started with the react-sprout router',
};

<HeroPattern />

# React sprout

React-sprout is a router for React.

## Installation {{ anchor: false }}

<CodeGroup>

```bash {{ title: 'npm' }}
npm install react-sprout
```

```bash {{ title: 'yarn' }}
yarn add react-sprout
```

</CodeGroup>

## Usage {{ anchor: false }}

```javascript
import React from 'react';
import Routes from 'react-sprout';

import { createRoot } from 'react-dom/client';

import Home from './home.jsx';
import Todos from './todos.jsx';

let Router = Routes(
	<>
		<Home path="home" />
		<Todos path="todos" />
	</>,
);

function Application() {
	return <Router />;
}

createRoot(document.getElementById('root')).render(<Application />);
```

<div className="not-prose flex flex-col items-end">
	<Button href="/guides/router" variant="text" arrow="right">
		Get started
	</Button>
</div>
