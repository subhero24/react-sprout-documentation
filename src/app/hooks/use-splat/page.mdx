# useSplat

A hook to retrieve the url splat.

```javascript
import { useSplat } from 'react-sprout';

let splat = useSplat();
```

<Properties>
	<Property name="splat" type="Array">
		An array of the remaining url segments of the route
	</Property>
</Properties>
